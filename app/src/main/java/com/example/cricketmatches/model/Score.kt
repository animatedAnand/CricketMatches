package com.example.cricketmatches.model

data class Score(
    // creating variables for runs,
    // wickets, overs and innings.
    var runs: Int,
    var wickets: Int,
    var overs: Int,
    var innings: String)
